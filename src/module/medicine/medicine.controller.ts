import { NextFunction, Request, Response } from "express";

const postMedicine=(
  err: unknown,
  req: Request,
  res: Response,
  next: NextFunction
) => {


    try {
        
    } catch (error) {
        
    }
}